# Copy to .env (same folder as docker-compose.yml) and fill in real values.
# .env is gitignored; this file documents what docker-compose.yml reads.

# --- Internal auth / multitenancy (all P1-hardened services) ---
# One secret per (issuer, audience) pair, not a single shared secret - a compromised
# service can no longer forge tokens for a pair it isn't part of. Each service is
# configured with an allow-list of expected callers (its InboundSecrets/inbound_secrets)
# and, if it calls other services, the secrets it signs outbound tokens with
# (OutboundSecrets/outbound_secrets). docker-compose.override.yml wires each variable
# below into BOTH ends of its pair. All fail Compose interpolation immediately if unset.
# Generate each one independently without versioning it:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
INTERNAL_AUTH_SECRET_WHATSAPP_BFF__CONVERSATION_ORCHESTRATOR=
INTERNAL_AUTH_SECRET_CONVERSATION_ORCHESTRATOR__WHATSAPP_BFF=
INTERNAL_AUTH_SECRET_CONVERSATION_ORCHESTRATOR__AGENT_RUNTIME_RENEGOTIATION=
INTERNAL_AUTH_SECRET_CONVERSATION_ORCHESTRATOR__CONVERSATION_AUDIT_SERVICE=
INTERNAL_AUTH_SECRET_CONVERSATION_ORCHESTRATOR__CONVERSATION_HANDOFF_SERVICE=
INTERNAL_AUTH_SECRET_CONVERSATION_ORCHESTRATOR__CONVERSATION_MEMORY_SERVICE=
INTERNAL_AUTH_SECRET_AGENT_RUNTIME_RENEGOTIATION__TOOL_SERVICE_RENEGOTIATION=
INTERNAL_AUTH_SECRET_AGENT_RUNTIME_RENEGOTIATION__KNOWLEDGE_SERVICE=
INTERNAL_AUTH_SECRET_AGENT_RUNTIME_RENEGOTIATION__CONVERSATION_MEMORY_SERVICE=
INTERNAL_AUTH_SECRET_TOOL_SERVICE_RENEGOTIATION__RENEGOTIATION_SERVICE=
INTERNAL_AUTH_SECRET_AGENT_RUNTIME_FATURA_CARTAO__TOOL_SERVICE_CARTAO_CREDITO=
INTERNAL_AUTH_SECRET_CONVERSATION_ORCHESTRATOR__AGENT_RUNTIME_FATURA_CARTAO=
INTERNAL_AUTH_SECRET_TOOL_SERVICE_CARTAO_CREDITO__CORE_BANCARIO_MOCK=
DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000001

# --- WhatsApp Cloud API (whatsapp-bff) ---
# Required to actually send/receive real WhatsApp messages. AppSecret/VerifyToken already
# have local-dev defaults in docker-compose.yml, so the webhook works without these for
# local testing with synthetic payloads. Keep the Graph API version explicit so upgrades
# can be validated independently from application releases.
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_APP_SECRET=local-dev-app-secret
WHATSAPP_VERIFY_TOKEN=local-dev-verify-token
WHATSAPP_GRAPH_API_VERSION=v20.0

# --- OpenAI (both agent runtimes) ---
# MOCK_AGENT_ENABLED defaults to true in docker-compose.yml, so /process returns a canned
# decision instead of calling OpenAI. The snapshot is fixed for homologation reproducibility;
# change it only together with the agent evaluation suite.
MOCK_AGENT_ENABLED=true
OPENAI_MODEL_ID=gpt-4o-mini-2024-07-18
OPENAI_API_KEY=

# --- Overrides, only needed if you stand these services up elsewhere ---
# docker-compose.yml's own defaults already point at the real containers in this workspace
# (knowledge-service, conversation-handoff-service, conversation-audit-service) - these two
# variables only matter if you want to point at a different deployment instead.
KNOWLEDGE_SERVICE_BASE_URL=http://knowledge-service:8500
HANDOFF_SERVICE_BASE_URL=http://conversation-handoff-service:8080
