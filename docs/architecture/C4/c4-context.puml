@startuml
!include <C4/C4_Context>

title C4 Context — Estado implementado

Person(customer, "Cliente", "Interage pelo WhatsApp nas jornadas implementadas.")

System_Ext(whatsapp, "WhatsApp Cloud API", "Entrega webhooks e recebe mensagens outbound.")
System_Ext(openai, "OpenAI", "Modelos de linguagem e embeddings.")
System_Ext(coreMock, "Core Bancário Mock", "APIs locais de renegociação, limite e fatura.")

System(platform, "Plataforma de IA Conversacional", "Orquestra WhatsApp, agentes, MCP, RAG, memória, auditoria e handoff persistido.")

Rel(customer, whatsapp, "Envia e recebe mensagens", "WhatsApp")
Rel(whatsapp, platform, "Webhook e mensagens outbound", "HTTPS")
Rel(platform, openai, "Invoca modelo e embeddings", "HTTPS")
Rel(platform, coreMock, "Consulta, simula, formaliza e consulta cartão", "HTTP")

SHOW_LEGEND()
@enduml
